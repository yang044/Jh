name: blank

on:
  push:

jobs:
  occasional:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        award: ["dusk", "appoint", "identify", "horizontal", "left"]
        superb: ["alter", "pea", "mill", "dissolve"]
    steps:
    - uses: actions/checkout@v6
    - uses: actions/setup-java@v5
      with:
        distribution: 'zulu'
        java-version: '25'
    - run: java -jar Mz
